
# mcpservers with adding tool


Add the following installation command into your client code:

"mcp-server": {
      "add tool": "uvx",
      "args": [
        "--from",
        "git+https://github.com/xhpentest-max/mcpservers.git",
        "mcp-server"
      ]
    }